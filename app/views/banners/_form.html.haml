= simple_form_for(@banner) do |f|
  = render 'layouts/error_messages', :target => f.object

  .form-inputs
    = f.input :name
    = f.input :location
    = f.input :image
    = f.input :link

  .form-actions
    = f.button :submit
