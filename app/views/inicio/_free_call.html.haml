= form_tag(home_path, :method => 'get') do
  = text_field_tag 'dest', nil , {:class => 'text-box', :size => '15', :placeholder => "número"}
  -# = image_submit_tag 'boton_llamar.png', :class => 'call-button'
  = image_submit_tag(image_path('btn-llama.png'))
