= simple_form_for(@product) do |f|
  = render 'layouts/error_messages', :target => f.object

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :image
    = image_tag @product.image.thumb if @product.image.present?

  .form-actions
    = f.button :submit
