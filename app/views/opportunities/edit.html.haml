#section-opportunities.container

  .section
    .title
      %h1
        = page_title!('crud.edit'+ ' ' + 'opportunity')

  .section
    = render 'form'

  .section
    .admin-controls
      = link_to 'crud.show', @opportunity, class: 'btn btn-info'
      = link_to 'crud.back', 'javascript:history.back(-1)', class: 'btn btn-warning'
