#section-contacts.container

  .section
    .title-page.admin-title
      %h3
        = page_title!('crud.edit'+ ' ' + 'contact')

  .section
    = render 'form'

  .section
    .admin-controls
      = link_to 'crud.show', @contact, class: 'btn btn-info'
      = link_to 'crud.back', 'javascript:history.back(-1)', class: 'btn btn-warning'
