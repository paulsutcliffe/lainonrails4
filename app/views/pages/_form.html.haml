= simple_form_for(@page) do |f|
  = render 'layouts/error_messages', :target => f.object

  .form-inputs
    = f.input :title
    = f.input :content, as: "ckeditor"

  .form-actions
    = f.button :submit
