#section-pages.container

  .section
    .title
      %h1
        = page_title!('crud.edit'+ ' ' + 'page')

  .section
    = render 'form'

  .section
    .admin-controls
      = link_to 'crud.show', @page, class: 'btn btn-info'
      = link_to 'crud.back', 'javascript:history.back(-1)', class: 'btn btn-warning'
