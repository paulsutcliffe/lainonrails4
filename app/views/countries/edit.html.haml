#section-countries.container

  .section
    .title
      %h1
        = page_title!('Editar Pais')

  .section
    = render 'form'

  .section
    .admin-controls
      = link_to 'Mostrar', @country, class: 'btn btn-info'
      = link_to 'crud.back', 'javascript:history.back(-1)', class: 'btn btn-warning'
